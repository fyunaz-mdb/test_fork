{
  "name": "test_fork",
  "version": "1.0.0",
  "description": "A very basic example of a Node.js project manifest.",
  "scripts": {
    "check": "git config --global user.email \"d@d.d\";git config --global user.name \"batman\";git pull;git checkout main;touch a;git add a; git commit -m \"aaaa\";git push origin main",
    "test": "echo \"test\""
  },
  "keywords": [
    "example"
  ],
  "dependencies": {
    "nanoid": "^5.1.6"
  }
}